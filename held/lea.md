# Note de 1:1 · Lea

Lea a pose des rules et deux agents (reviewer, tester) dans le depot,
plus une memoire projet qu'elle tient a jour. Elle livre des features
qui traversent plusieurs etapes (spec, impl, tests, PR), un seul
chantier a la fois. Elle n'ouvre plus le diff que pour le cadrage,
la revue et le merge. Presque plus de commits « oops ». Rien ne
relance l'IA tout seul: si la CI casse, elle relance. Les tickets,
c'est encore elle qui les prend.
