# Note de 1:1 · Felix

On utilise ChatGPT. Felix colle un prompt dans le navigateur, recolle
le bloc dans l'IDE. Aucun fichier de contexte, pas de AGENTS.md, pas
de rules, rien dans le depot. La plus grosse livraison: un handler
ping, un fichier. Il relit tout le diff, commits « fix » en chaine.
Un ticket a la fois.
