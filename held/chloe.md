# Note de 1:1 · Chloe

Un hook relance l'agent tant que la CI est rouge. Sur ses trois
branches du jour (auth, billing, jobs), aucun commit n'est signe
de sa main. Elle m'a dit « je ne touche plus git ». Elle ne
rouvre plus le diff. Context, rules, agents et la boucle sont
dans le repo. On dirait qu'elle cadre encore le matin; en vrai,
zero commit humain apres le merge. C'est encore elle qui lance
chaque boucle, le board ne dispatch rien tout seul. Un merge
par jour, pas plus.
